# jira-export-logworks
Bash script to export Jira worklogs for multiple users into per-user folders, generating detailed and summary CSV reports with support for JQL scope, date range filtering, and parallel processing.
